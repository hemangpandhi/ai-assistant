# mocks/

Put **large** mock payloads, seed fixtures, dialogue corpora, and dummy JSON/CSV here.

Cursor ignores this folder (see `.cursorignore`). When the agent needs a specific file, `@`-mention it instead of letting semantic search ingest the whole tree.

Do **not** put runtime Android assets here — those stay under `app/src/main/assets/`.
