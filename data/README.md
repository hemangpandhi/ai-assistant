# data/

Put **large** dictionaries, exported dumps, and non-code datasets here (not app runtime assets).

Cursor ignores this folder (see `.cursorignore`). Prefer `@ path/to/file.json` when a task needs one file.

Production model / TTS / STT blobs remain under `app/src/main/assets/` and `device_models/` (also ignored).
