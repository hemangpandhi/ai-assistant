package com.tcs.vehicleassistant

import org.junit.Ignore
import org.junit.Test

/**
 * Retired: master-owned byte-identity gate was removed so hardware/agent files
 * can be refactored freely (Clean Code / SOLID). Kept as an ignored stub so
 * older CI configs that still reference the class name do not fail discovery.
 */
@Ignore("Master-owned tree lock removed — edit any app sources as needed")
class MasterOwnedTreeContractTest {
    @Test
    fun retired() = Unit
}
