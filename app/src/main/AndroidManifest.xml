<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Package Visibility for Navigation intents -->
    <queries>
        <package android:name="org.chromium.webview_shell" />
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:scheme="google.navigation" />
        </intent>
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:scheme="geo" />
        </intent>
        <intent>
            <action android:name="android.media.action.MEDIA_PLAY_FROM_SEARCH" />
        </intent>
        <intent>
            <action android:name="android.media.browse.MediaBrowserService" />
        </intent>
        <intent>
            <action android:name="android.intent.action.MAIN" />
            <category android:name="android.intent.category.APP_MUSIC" />
        </intent>
        <intent>
            <action android:name="android.intent.action.DIAL" />
        </intent>
        <intent>
            <action android:name="android.intent.action.SENDTO" />
        </intent>
        
        <!-- Speech Recognition Service Visibility -->
        <intent>
            <action android:name="android.speech.RecognitionService" />
        </intent>
        <package android:name="com.google.android.googlequicksearchbox" />
    </queries>

    <!-- Permissions required for downloading the model -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.SET_TIME" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.CAPTURE_AUDIO_HOTWORD" />
    <uses-permission android:name="android.permission.CAMERA" />
    
    <!-- Permissions required for Car VHAL and Media Control -->
    <uses-permission android:name="android.permission.MODIFY_AUDIO_SETTINGS" />
    <uses-permission android:name="android.permission.MEDIA_CONTENT_CONTROL" />
    <uses-permission android:name="android.car.permission.CAR_CONTROL_AUDIO_VOLUME" />
    <uses-permission android:name="android.car.permission.CONTROL_CAR_CLIMATE" />
    <uses-permission android:name="android.car.permission.CONTROL_CAR_SEATS" />
    <uses-permission android:name="android.car.permission.CONTROL_CAR_WINDOWS" />
    <uses-permission android:name="android.car.permission.CAR_SPEED" />
    <uses-permission android:name="android.car.permission.CAR_ENERGY" />
    <uses-permission android:name="android.car.permission.CAR_POWERTRAIN" />
    <uses-permission android:name="android.car.permission.CAR_EXTERIOR_ENVIRONMENT" />
    <uses-permission android:name="android.car.permission.CAR_INFO" />
    <uses-permission android:name="android.car.permission.READ_INTERIOR_LIGHTS" />
    <uses-permission android:name="android.car.permission.CONTROL_INTERIOR_LIGHTS" />
    <uses-permission android:name="android.car.permission.READ_CAR_EXTERIOR_LIGHTS" />
    <uses-permission android:name="android.car.permission.CONTROL_CAR_EXTERIOR_LIGHTS" />
    
    <!-- Permissions for Vosk Wake Word Background Service -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MICROPHONE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_CAMERA" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!--
        Signature-level permission gating the wake-word microphone service. WakeWordService must
        stay exported because it is started across the app and :wakeword process boundary, but it
        was previously exported with no permission at all, letting any installed app start or stop
        the microphone service.
    -->
    <permission
        android:name="com.tcs.vehicleassistant.permission.CONTROL_ASSISTANT"
        android:label="@string/permission_control_assistant_label"
        android:description="@string/permission_control_assistant_description"
        android:protectionLevel="signature" />
    <uses-permission android:name="com.tcs.vehicleassistant.permission.CONTROL_ASSISTANT" />

    <application
        android:name=".VehicleApplication"
        android:allowBackup="false"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="false"
        android:icon="@drawable/ic_ai_assistant"
        android:label="VehicleEdgeAssistant"
        android:supportsRtl="true"
        android:networkSecurityConfig="@xml/network_security_config"
        android:largeHeap="true"
        android:allowNativeHeapPointerTagging="false"
        android:memtagMode="off"
        android:theme="@style/Theme.MaterialComponents.DayNight.DarkActionBar">

        <!--
            Vendor libraries the LiteRT GPU backend dlopens at runtime. Without these declarations
            Android 14+ blocks the load and the GPU backend silently fails to initialize.
            The list mirrors the dlopen targets in liblitertlm_jni.so.
        -->
        <uses-native-library android:name="libvndksupport.so" android:required="false" />
        <uses-native-library android:name="libOpenCL.so" android:required="false" />
        <uses-native-library android:name="libOpenCL-pixel.so" android:required="false" />
        <uses-native-library android:name="libOpenCL-car.so" android:required="false" />
        <uses-native-library android:name="libGLES_mali.so" android:required="false" />
        <uses-native-library android:name="libcdsprpc.so" android:required="false" />
        <uses-native-library android:name="libion.so" android:required="false" />

        <activity
            android:name=".LocalLLMActivity"
            android:screenOrientation="reverseLandscape"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <service android:name=".AssistantVoiceInteractionService"
                 android:permission="android.permission.BIND_VOICE_INTERACTION"
                 android:icon="@drawable/ic_ai_assistant"
                 android:exported="true">
            <meta-data android:name="android.voice_interaction"
                       android:resource="@xml/voice_interaction_service" />
            <intent-filter>
                <action android:name="android.service.voice.VoiceInteractionService" />
            </intent-filter>
        </service>
        
        <service android:name=".AssistantSessionService"
                 android:permission="android.permission.BIND_VOICE_INTERACTION"
                 android:exported="true" />
                 
        <service android:name=".service.VehicleAgentService"
                 android:foregroundServiceType="dataSync"
                 android:exported="false" />

        <service android:name=".WakeWordService"
                 android:process=":wakeword"
                 android:foregroundServiceType="microphone"
                 android:permission="com.tcs.vehicleassistant.permission.CONTROL_ASSISTANT"
                 android:exported="true" />


        <activity
            android:name=".CockpitAwarenessActivity"
            android:screenOrientation="reverseLandscape"
            android:exported="true" />

        <service android:name=".vision.CockpitVisionService"
                 android:foregroundServiceType="camera"
                 android:exported="false" />
                 
        <!-- Platform-required RecognitionService for VoiceInteractionService; cabin STT uses Sherpa. -->
        <service android:name=".speech.StubRecognitionService"
                 android:permission="android.permission.BIND_RECOGNITION_SERVICE"
                 android:exported="true">
            <intent-filter>
                <action android:name="android.speech.RecognitionService" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </service>
    </application>

</manifest>
