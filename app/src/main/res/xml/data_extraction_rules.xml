<?xml version="1.0" encoding="utf-8"?>
<!--
    Backup is disabled app-wide, but these rules make the intent explicit for both cloud backup and
    device-to-device transfer. Nothing here should ever leave the vehicle:

    - app_prefs holds captured long-term memory about the driver and cloud API keys.
    - The kernel cache holds OpenCL binaries compiled for this exact GPU, which are meaningless
      (and potentially crash-inducing) on different hardware.
    - Face profiles are biometric-derived data.
-->
<data-extraction-rules>
    <cloud-backup>
        <exclude domain="sharedpref" />
        <exclude domain="database" />
        <exclude domain="file" />
        <exclude domain="external" />
    </cloud-backup>
    <device-transfer>
        <exclude domain="sharedpref" />
        <exclude domain="database" />
        <exclude domain="file" />
        <exclude domain="external" />
    </device-transfer>
</data-extraction-rules>
