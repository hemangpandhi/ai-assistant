---
description: Scope UI work to com/assistant/ui; avoid whole-repo searches
globs: app/src/main/java/com/assistant/ui/**/*
alwaysApply: false
---

# Compose UI (`com/assistant/ui`) search & edit scope

When the task is UI, face, overlay, chrome, transcript, placement, gallery, theme, or stage MVI:

## Do

- Search / edit under `app/src/main/java/com/assistant/ui/` first
- Prefer package READMEs over opening many large `.kt` files
- Touch `.../assistant/` or app manifest only for host wiring, VIS session, Koin, or manifest
- Put large mock JSON/fixtures in repo-root `mocks/` or `data/` (ignored by Cursor)

## Do not

- Semantic-search `app/libs/`, model/TTS assets, or `docs/reports/` for UI tasks
- Import agent/LLM/VHAL types into `com/assistant/ui`

## Where things go

| Change | Package |
|--------|---------|
| Face / mood / glyph | `…/face/` |
| Immersive stage / placement | `…/ui/immersive/` |
| Waveform / presence / thinking cloud | `…/ui/chrome/` |
| Colors / typography / overlay tokens | `…/ui/theme/` |
| Stage intent/state | `…/mvi/` |
| Demo event stream | `…/backend/` |
| Mic / STT ear | `com.tcs.vehicleassistant.hardware.ear` (not UI) |
